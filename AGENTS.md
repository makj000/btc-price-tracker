* Goal
use CoinMarketCap API to build a cronjob to run on the background and check bitcoin price for every 5 mins, and send me a SMS if it reaches preset threshold.

* UI
index.html page to report price history, to allow setting thresholds (high and low)
